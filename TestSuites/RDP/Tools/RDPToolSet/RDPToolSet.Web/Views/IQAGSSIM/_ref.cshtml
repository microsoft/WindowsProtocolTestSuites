<div id="ref" class="text-left">
    <h2>Reference</h2>
    <p>
        G-SSIM leverages the edge information to assess the image quality. The gradient map of an image is
         generated by using the Sobel operator. The figure below shows the two 3x3 masks of Sobel operators
         used in G-SSIM.
    </p>
    <div class="text-center" style="padding: 5px;">
        <img src="~/Images/Resource/GSSIM/SobelOperator.png" />
    </div>
    <p>
        For each pixel <i>p<sub>i,j</sub></i>, its gradient vector is defined as <span>
            <img src="~/Images/Resource/GSSIM/Eij.png" /></span>, where <i>dx<sub>i,j</sub></i> and <i>dy<sub>i,j</sub></i>
         are obtained by the vertical edge mask and horizontal edge mask respectively, and the magnitude of this vector is
         roughly given by:
    </p>
    <div class="text-center" style="padding: 5px;">
        <img src="~/Images/Resource/GSSIM/Magij.png" />
    </div>
    <p>
        All the pixels' gradient form the image's gradient map. The edge and contour information of an image can be highlighted
         in its gradient map.
    </p>
    <p>
        We set <b>X'</b> and <b>Y'</b> represent the gradient map of the original image and the distorted one respectively,
         and let <b>x'</b> and <b>y'</b> be the block vectors of <b>X'</b> and <b>Y'</b>, then gradient-based contrast comparison
         <i>C<sub>g</sub>(x, y)</i> and structure comparison <i>C<sub>g</sub>(x, y)</i> can be described by:
    </p>
     <div class="text-center" style="padding: 5px;">
        <img src="~/Images/Resource/GSSIM/CgSg.png" />
    </div>
    <p>
        where &sigma;<sub>x'</sub> and &sigma;<sub>y'</sub> are the standard deviation of vector <b>x'</b> and <b>y'</b> respectively,
         &sigma;<sub>x'y'</sub> is the covariance of vector <b>x'</b> and <b>y'</b>, and C<sub>2</sub>, C<sub>3</sub> are small constants
         to avoid the denominator being zero. And the Gradient-based Structural Similarity(GSSIM) is described as follows:
    </p>
    <div class="text-center" style="padding: 5px;">
        <img src="~/Images/Resource/GSSIM/GSSIM.png" />
    </div>
    <p>
        The overall image structure similarity is calculated as the mean of all the subimages GSSIM.
    </p>
     <div class="text-center" style="padding: 5px;">；
        <img src="~/Images/Resource/GSSIM/MGSSIM.png" />
    </div>
    <p>
        Details please refer to this paper <a href="http://ieeexplore.ieee.org/xpl/articleDetails.jsp?tp=&arnumber=4107183"><i>Gradient-Based
             Structural Similarity for Image Quality Assessment</i></a>.
    </p>
</div>
